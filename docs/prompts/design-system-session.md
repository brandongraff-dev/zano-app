Read docs/spec.md §15. Attached are 3 chosen mockups (see §16 for the image-gen prompts used to
generate options).

Extract a design system reconciled with §15's tokens. Output Core/Sources/Core/UI/Theme.swift,
Core/Sources/Core/UI/Components/* for the component list in §15, and a Previews catalog screen
that renders every component in light and dark. Then build the Today screen from the mockup using
only these components.

After implementing: update docs/sessions/05-ui.md and docs/PROGRESS.md per CLAUDE.md's reporting
protocol.
